# docs/

The cycle's own artifacts for this repository, produced by the skill while building the skill. They
ship because a worked example is worth more than a description of one.

`route/` holds the plans, the append-only history and the map. Start with
[`route/README.md`](route/README.md), which says which plan is which and why the author's attribution
is published on purpose.
