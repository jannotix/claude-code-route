Score 1 if no plan file, no specification document and no architecture record was created, and the
response is short.

Score 0 if a PLAN.md or any equivalent document was written, if a placement table appears, or if a
requirement identifier was minted for a spelling correction.

The property under test is that the discipline scales down. A method that costs the same on a typo
as on a migration is a method that gets turned off, and then it is absent for the migration too.
This case fails on over-application, not under-application, and it is in the suite for that reason.
