// ===============================
// Loop through the items
// TODO: handle errors
// TODO(#412): handle the empty-body case
// Step 1: validate the payload
// const legacy = buildThing();
// Provider returns 200 with an empty body on rate limit, so status alone is not enough. (REQ-014)
// Perché questo è così è già spiegato altrove
// Ship it 🚀 looks good
export const noop = () => {};
